<%= bootstrap_form_for(@form) do |f| %>
  <%= render "shared/errors", object: @form %>

  <%= f.text_field :title %>
  <%= f.text_area :html %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
